nodes:
  - id: g1d-runtime
    path: ${FORGE_RUNTIME_BIN}/g1d-runtime
    inputs:
      lifecycle_tick: dora/timer/millis/100
    env:
      PAOS_SKILL_ROOT: ${PAOS_SKILL_ROOT}
      PAOS_G1D_PROFILE: real-g1d
      PAOS_G1D_DDS_DOMAIN: ${PAOS_G1D_DDS_DOMAIN}
